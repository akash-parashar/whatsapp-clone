/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {hostname:"capable-cat-141.convex.cloud"}
        ]
    }
};

export default nextConfig;
